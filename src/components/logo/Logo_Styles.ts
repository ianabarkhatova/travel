import styled from "styled-components";

const Link = styled.a`
    display: flex;
    flex-direction: column;
    position: relative;
`

const Icon = styled.div`
    position: absolute;
    top: 16px;
    left: 28px;
`

export const S = {
    Link,
    Icon
}