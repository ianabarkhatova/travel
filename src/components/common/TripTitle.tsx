import styled from "styled-components";
import {font} from "../../styles/Common";

export const TripTitle = styled.h3`
    ${font({
        weight: 500,
        Fmax: 18,
    })}
    
    color: #080809;
`